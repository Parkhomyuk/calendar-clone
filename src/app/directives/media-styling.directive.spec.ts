import { MediaStylingDirective } from './media-styling.directive';

describe('MediaStylingDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaStylingDirective();
    expect(directive).toBeTruthy();
  });
});
